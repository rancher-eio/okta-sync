query Query(
  $enterpriseSlug:String!,
  $first:Int=100,
  $after:String,
) {
  enterprise(slug:$enterpriseSlug) {
    ownerInfo {
      pendingMemberInvitations(first:$first,after:$after) {
        pageInfo {
          endCursor
          hasNextPage
        }
        nodes {
          email
          id
          invitee {
            databaseId
            email
            id
            login
          }
          inviterActor {
            databaseId
            email
            id
            login
          }
          organization {
            databaseId
            id
            login
          }
        }
      }
    }
  }
}